services:
  backend:
    image: cabreraevil/openai-cli
    build:
      context: .
      dockerfile: Dockerfile
    container_name: openai-cli
    restart: no
    environment:
      - NODE_ENV=${NODE_ENV-development}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
